name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  create_release:
    name: Create Release
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      with:
        submodules: recursive
    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        name: teloscope ${{ github.ref_name }}
        generate_release_notes: true
        draft: false
        prerelease: false

  add_resources:
    needs: create_release
    name: Add Resources
    permissions: write-all
    strategy:
      matrix:
        OS: [macos-latest, ubuntu-latest, windows-2025]
        include:
        - OS: macos-latest
          OS_NAME: macOS
        - OS: ubuntu-latest
          OS_NAME: linux
        - OS: windows-2025
          OS_NAME: win
    runs-on: ${{ matrix.OS }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
            submodules: true
      - name: Build
        run: make
      - name: Zip
        id: package
        run: |
          asset="${RUNNER_TEMP}/teloscope.${{ github.ref_name }}-${{ matrix.OS_NAME }}.zip"
          if [ "$RUNNER_OS" = "Windows" ]; then
            powershell -NoProfile -Command "Compress-Archive -Path 'build/bin/teloscope*' -DestinationPath '${asset}'"
          else
            zip -j "$asset" build/bin/teloscope*
          fi
          echo "asset=$asset" >> "$GITHUB_OUTPUT"
        shell: bash
      - name: Upload binaries
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.package.outputs.asset }}

  add_submodules:
    needs: create_release
    name: Source with submodules
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
            submodules: true
      - name: Zip
        id: package
        run: |
          asset="${RUNNER_TEMP}/teloscope.${{ github.ref_name }}-with_submodules.zip"
          zip -r "$asset" . -x ".git/*"
          echo "asset=$asset" >> "$GITHUB_OUTPUT"
      - name: Upload source
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.package.outputs.asset }}
